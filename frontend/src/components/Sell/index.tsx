import SellForm from '../SellForm';
import { Flex } from 'antd';

const Sell = () => {
    return (
        <Flex justify="center">
            <SellForm />
        </Flex>
    );
};

export default Sell;
